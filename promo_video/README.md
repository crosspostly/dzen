## 🎬 Promo Video Factory v3.0

Автоматизированная система создания **виральных Shorts/Reels** для статей Дзена.
Полный цикл: `Статья -> Сценарий -> Озвучка -> Монтаж -> Публикация`.

---

## 🔥 Новые возможности (v3.0)

### 1. 🎭 Cinematic Visuals (9:16)
- Специальный режим генерации изображений для видео.
- **Strict 9:16:** Использование настроек API для вертикального формата (никакого кропа 16:9).
- **Стиль:** Netflix Drama, 8k, Dramatic Lighting.
- **Anti-Trash:** Убраны артефакты "любительского фото", которые используются в статьях.

### 2. 🗣️ Single Pass Audio
- Озвучка генерируется **единым файлом** для всего ролика.
- Результат: Естественная интонация, правильные паузы, отсутствие "роботизированных" переходов между сценами.
- Видеоряд автоматически подстраивается под длину аудио.

### 3. 🚀 Auto-Publishing (Playwright)
- Автоматическая загрузка видео в Дзен Студию.
- Заполнение заголовка, описания и тегов.
- Загрузка обложки.
- Публикация.

---

## 💎 Технические нюансы (Важно)

### 🖼️ Умные Обложки (00:00)
Система автоматически создает первый кадр видео:
- **Auto-Scaling:** Если заголовок слишком длинный, шрифт автоматически уменьшается (со 120px до 60px), чтобы текст не выходил за границы безопасной зоны.
- **Visual Impact:** Используется затемнение фона и жирный шрифт Roboto (Black) для максимальной читаемости на любом фоне.

### 📁 Организация файлов
- **Имя видео:** Всегда совпадает с именем статьи (например, `moia-istoria.mp4`).
- **Скрытые ассеты:** Все временные файлы (озвучка по частям, отдельные кадры) хранятся в скрытой папке `.assets_<название_статьи>`, чтобы не загромождать репозиторий.
- **Лимит GitHub:** В GitHub Actions установлена проверка на **25 МБ**. Если видео превышает этот размер, оно не будет загружено в репозиторий (защита от ошибок Git).

### 🤖 Работа в GitHub Actions
Воркфлоу `auto-video-generation.yml` работает по принципу "обнаружил и исправил":
1. Реагирует на пуш любой статьи в `articles/`.
2. Сканирует папку и находит `.md` файлы без соответствующего `.mp4`.
3. Генерирует видео только для новых или измененных материалов.

---

## 🛠️ Как запустить

### Генерация видео
```bash
npx tsx promo_video/src/cli.ts <путь_к_статье.md>
```

### Генерация + Публикация в Дзен
```bash
npx tsx promo_video/src/cli.ts <путь_к_статье.md> --publish
```
*Требует файл `config/cookies.json` с актуальной сессией.*

---

## 📂 Результаты
Артефакты (видео, аудио, картинки) сохраняются в папке статьи: `articles/.../video_assets_<slug>/`

### 📺 Просмотр результата (Локально)
Чтобы просмотреть видео через браузер:
1. Видео автоматически копируется в `public/generated_videos/`.
2. Ссылка для просмотра:
   ```
   http://crosspostly.hopto.org:5005/generated_videos/<имя_файла>.mp4
   ```

---

## 🕵️ Техническая документация
- **Стратегия поиска селекторов:** [docs/SELECTOR_STRATEGY.md](docs/SELECTOR_STRATEGY.md) — как мы научили робота нажимать кнопки в React-приложении.
- **Спецификация:** [SPECIFICATION.md](SPECIFICATION.md).

## Правила (Strict)
1.  **Только Gemini 2.5 Flash** для генерации сценариев и озвучки.
2.  **Никаких сторонних TTS** (edge-tts удален).
3.  **Аудио** генерируется через API (`response_modalities: ["AUDIO"]`).

