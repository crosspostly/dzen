# 🚀 Быстрый Старт RSS Генератора

## Что это?

**Двухрежимный генератор RSS-ленты** для опубликованных статей с:
- ✅ Проверкой путей к изображениям
- ✅ Автоматической дедупликацией
- ✅ Перемещением новых статей в `published`
- ✅ Полной и инкрементальной синхронизацией

---

## Быстрый Запуск

### 🔄 Режим 1: FULL (Полная регенерация)

**Используй когда:** исправил пути, обновил конфиг, нужна свежая лента

```bash
# Локально
npm run feed:full

# Или через GitHub Actions (Actions → Manual Feed Generation → full)
```

**Результат:**
- Сканирует **ВСЕ** статьи из `articles/published/`
- Перезаписывает `feed.xml` с нуля
- Проверяет все пути и изображения

### 📮 Режим 2: INCREMENTAL (Только новые)

**Используй когда:** добавил новые статьи через content-factory

```bash
# Локально
npm run feed:incremental

# Или
node scripts/generate-feed.js incremental
```

**Результат:**
- Сканирует новые статьи из `articles/`
- Добавляет все опубликованные из `articles/published/`
- Перемещает новые в `published` (+ изображения)
- Удаляет исходные файлы

---

## Примеры Использования

### После запуска content-factory

```bash
# 1️⃣ content-factory создал новые статьи в articles/2025/12/
# 2️⃣ Запусти инкрементальный режим
npm run feed:incremental
# 3️⃣ Статьи переместятся в published/
# 4️⃣ feed.xml обновится автоматически
```

### Утренний запуск (суточная синхронизация)

```bash
# Запусти в GitHub Actions каждый день утром
# Actions → Manual Feed Generation → full
# Это гарантирует, что все пути правильные
```

### Исправление сломанных путей

```bash
# Если кто-то вручную переместил статьи
npm run feed:full
# Все пути обновятся автоматически
```

---

## Что Проверяется?

### ✅ Валидация Изображений

```
✅ ya-dumala-chto-schastliva...
   🖼️  https://raw.githubusercontent.com/crosspostly/dzen/main/articles/published/2025/12/21/image-cover.jpg
```

### ⚠️ Ошибки

```
❌ ОШИБКА: title отсутствует - статья пропущена
26️⃣ ДУБЛИКАТ: ya-dumala-chto-schastliva - пропущена
🖼️ НЕ НАЙДЕНО изображение: missing-photo.jpg
```

---

## Статистика

```
===== СТАТИСТИКА ===== [🔄 FULL]
📊 Всего файлов: 47
✅ Обработано: 47
⚠️ Пропущено: 0
🖼️ Ошибки изображений: 0

📋 RSS-лента сохранена: feed.xml (47 статей)
```

---

## Команды

| Команда | Описание |
|---------|----------|
| `npm run feed:full` | Полная регенерация из `articles/published` |
| `npm run feed:incremental` | Новые + опубликованные |
| `npm run feed:generate` | Алиас для `incremental` |
| `node scripts/generate-feed.js full` | Прямой запуск (full) |
| `node scripts/generate-feed.js incremental` | Прямой запуск (incremental) |

---

## GitHub Actions

1. Открой репозиторий
2. **Actions** → **Manual Feed Generation**
3. **Run workflow** (жёлтая кнопка)
4. Выбери режим:
   - `full` — полная регенерация
   - `incremental` — только новые
5. **Run**

> Результат увидишь в **Actions** → последний workflow → Summary

---

## Файлы

- `scripts/generate-feed.js` — основной скрипт
- `.github/workflows/manual-feed-generate.yml` — GitHub Actions
- `RSS_GENERATION.md` — подробная документация
- `feed.xml` — результат (генерируется)

---

## Проблемы?

### RSS не обновляется
```bash
# 1. Проверь логи
npm run feed:full

# 2. Если ошибки про изображения
# → Проверь что файлы реально в той же папке что markdown

# 3. Если пути неправильные
# → Используй feed:full чтобы обновить
```

### Изображения не загружаются
```bash
# Проверь что файл существует
ls -la articles/published/2025/12/21/

# Если файла нет → скопируй его туда
# Если имя не совпадает → переименуй

# Затем
npm run feed:full
```

### Статьи исчезли из RSS
```bash
# Возможно дубликаты были отфильтрованы
# Проверь логи на наличие ДУБЛИКАТ

# Или они ещё в articles/ (не в published)
npm run feed:incremental
```

---

## Расширенное

Подробная документация: [`RSS_GENERATION.md`](./RSS_GENERATION.md)

Когда использовать каждый режим: [`RSS_GENERATION.md#Режимы`](./RSS_GENERATION.md#мода-генерации)
