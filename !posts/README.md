# Dzen Auto-Publisher - Полная документация

> 🎯 **Production-ready инструмент для автоматической публикации статей в Яндекс Дзен**

## 📦 Структура проекта

```
!posts/
├── PRODUCTION_READY/           # ✅ ОСНОВНАЯ РАБОЧАЯ ВЕРСИЯ
│   ├── src/                    # Исходные коды
│   ├── config/                 # Конфигурация (cookies.json)
│   ├── modules/                # Модули (история публикаций)
│   ├── public/                 # Публичные файлы (feed.xml)
│   ├── dzen-schema.json        # Схема селекторов
│   ├── package.json            # Зависимости
│   └── *.md                    # Полная документация
├── zen_auto_publisher.js       # Основной скрипт
├── README.md                   # Этот файл (главная документация)
└── for_delete/                 # Архив старых версий (дубликаты, отладка)
```

## 🚀 Быстрый старт

```bash
cd PRODUCTION_READY
npm install
npm run install-browsers
npm start
```

---

## 🔑 КРИТИЧЕСКИ ВАЖНЫЕ СЕЛЕКТОРЫ

### Основные селекторы для работы с интерфейсом Дзен

| Элемент | Селектор | Назначение |
|---------|----------|-----------|
| **Кнопка добавления публикации** | `[data-testid="add-publication-button"]` | Открытие редактора |
| **Кнопка "Написать статью"** | `text="Написать статью"` | Создание новой статьи |
| **Поле заголовка** | `input[placeholder="Заголовок"]` | Ввод заголовка |
| **Поле содержимого** | `textarea[placeholder="Текст статьи"]` | Ввод текста |
| **Кнопка вставки изображения** | `button[data-tip="Вставить изображение"]` | Загрузка изображения |
| **Кнопка публикации (редактор)** | `[data-testid="publish-btn"]` | Первый клик публикации |
| **Кнопка публикации (модаль)** | `button[data-testid="publish-btn"][type="submit"]` | Подтверждение публикации |

### Как тестировать селекторы

1. Откройте DevTools в браузере (F12)
2. Перейдите в консоль и выполните:
```javascript
document.querySelector('ваш-селектор')
```
3. Если результат не null - селектор работает ✅

### Обновление селекторов при изменениях интерфейса

1. Откройте DevTools при работе с Дзен
2. Найдите нужный элемент в инспекторе
3. Обновите селектор в `src/main.js` (основной скрипт)
4. Протестируйте в консоли перед запуском
5. Запустите скрипт с `headless: false` для визуального контроля

---

## 📋 АЛГОРИТМ РАБОТЫ

### Основной процесс публикации

```
1. ✅ Инициализация
   ├─ Загрузка куки (аутентификация)
   ├─ Чтение feed.xml (RSS)
   └─ Проверка истории (published_articles.txt)

2. ✅ Для каждой статьи:
   ├─ Заполнение заголовка
   ├─ Заполнение текста (с обработкой HTML-тегов)
   ├─ Нажатие Enter (для активации интерфейса)
   ├─ Вставка изображения
   └─ Двухшаговая публикация:
      ├─ Клик на кнопку в редакторе
      └─ Клик на кнопку в модальном окне

3. ✅ Сохранение результатов
   ├─ Запись в published_articles.txt
   └─ Логирование в консоль
```

### Обработка HTML-тегов в тексте

Текст из RSS-файла содержит HTML-теги, которые корректно конвертируются:

| HTML тег | Преобразование | Результат |
|----------|----------------|-----------|
| `<p>...</p>` | Двойной перенос | `\n\n` |
| `<h1>-<h6>` | Двойной перенос | `\n\n` |
| `<div>` | Одинарный перенос | `\n` |
| `<br>` | Перенос строки | `\n` |
| `<li>` | Перенос с маркером | `\n• ` |
| `<b>, <strong>` | Сохранение текста | без изменений |
| `<i>, <em>` | Сохранение текста | без изменений |
| `&nbsp;` | Пробел | ` ` |
| `&amp;` | Амперсанд | `&` |

**Пример:**
```html
Входящий HTML:
<p>Привет <b>мир</b></p>
<p>Вторая строка</p>

Результат в Дзене:
Привет мир

Вторая строка
```

### Двухшаговая публикация

Почему нужно два клика:

1. **Клик в редакторе** (`[data-testid="publish-btn"]`)
   - Первая валидация контента
   - Проверка на пустые поля
   
2. **Клик в модальном окне** (`button[data-testid="publish-btn"][type="submit"]`)
   - Окончательное подтверждение
   - Публикация в ленту

Это критично для работы скрипта!

---

## 🔐 СХЕМА И КОНФИГУРАЦИЯ

### dzen-schema.json

Файл содержит:
- Точные CSS селекторы для элементов UI
- Xpath селекторы (альтернативные)
- Метаинформацию о структуре интерфейса

**Расположение:**
- `!posts/PRODUCTION_READY/dzen-schema.json`
- `!posts/dzen-schema.json` (дубль)

### Структура данных публикации

```javascript
{
  "title": "Заголовок статьи",           // Из RSS <title>
  "description": "Описание",              // Из RSS <description>
  "content": "Полный текст",              // Из RSS <content:encoded>
  "image": "https://example.com/img.jpg", // Из RSS <media:content>
  "link": "https://example.com/article",  // Из RSS <link>
  "pubDate": "2024-01-04T10:45:00Z"      // Дата публикации
}
```

### Конфиг зависимостей (package.json)

```json
{
  "dependencies": {
    "playwright": "^latest",
    "playwright-extra": "^3.x"
  },
  "scripts": {
    "start": "node src/main.js",
    "install-browsers": "playwright install"
  }
}
```

---

## 📁 ВАЖНЫЕ ФАЙЛЫ

### PRODUCTION_READY/ - Production версия

| Файл | Размер | Назначение |
|------|--------|-----------|
| `src/main.js` | 34.8 KB | **Основной скрипт публикации** |
| `modules/publication_history.js` | ? | Отслеживание истории |
| `dzen-schema.json` | 23.8 KB | Схема селекторов |
| `config/cookies.json` | ? | Аутентификационные куки |
| `public/feed.xml` | ? | RSS-фид со статьями |
| `package.json` | 1 KB | Зависимости npm |

### Документация в PRODUCTION_READY/

| Файл | Содержание |
|------|-----------|
| `README.md` | Основной гайд проекта |
| `INTRODUCTION.md` | Введение и возможности |
| `RUN_INSTRUCTIONS.md` | Инструкции по запуску |
| `UPDATE_INSTRUCTIONS.md` | **Как обновлять при изменении интерфейса** |
| `UPDATING_SELECTORS.md` | Детальное обновление селекторов |
| `SELECTORS_INFO.md` | Информация о селекторах |
| `MAIN_SCRIPT_INFO.md` | Описание структуры скрипта |
| `AUTOMATION.md` | Автоматизация запуска (GitHub Actions, Cron) |
| `SECURITY_AND_RECOMMENDATIONS.md` | Безопасность и best practices |
| `FAQ.md` | Часто задаваемые вопросы |
| `FILES_OVERVIEW.md` | Обзор всех файлов |

---

## ⚙️ КЛЮЧЕВЫЕ ФУНКЦИИ СКРИПТА

### getArticlesFromFeed()
Извлекает статьи из RSS-файла:
- Читает `public/feed.xml`
- Парсит XML с помощью regex
- Возвращает массив объектов с статьями
- Фильтрует по истории публикаций

### processArticleContent()
Конвертирует HTML в читаемый текст:
- Обрабатывает все HTML-теги
- Сохраняет структуру текста
- Удаляет лишние теги
- Заменяет HTML-сущности на символы

### Основной цикл публикации
```javascript
1. Инициализация браузера
2. Загрузка куки (аутентификация)
3. Открытие редактора Дзен
4. Для каждой статьи:
   - Заполнение полей
   - Обработка тегов
   - Вставка изображения
   - Публикация (2 клика)
5. Сохранение в историю
6. Закрытие браузера
```

---

## 🔍 МОНИТОРИНГ И ОТЛАДКА

### Файлы отслеживания

- `data/published_articles.txt` - История опубликованных статей
- Консоль вывода - Логирование процесса
- Browser screenshot - При ошибках (если включено)

### Режим отладки

Для визуального контроля отредактируйте `src/main.js`:
```javascript
const browser = await chromium.launch({
  headless: false  // Показывать окно браузера
});
```

### Типовые проблемы и решения

| Проблема | Решение |
|----------|---------|
| "Element not found" | Обновить селектор в UPDATE_INSTRUCTIONS.md |
| Статья не публикуется | Проверить аутентификацию (cookies.json) |
| Изображение не вставляется | Обновить селектор кнопки вставки |
| Публикация висит | Добавить timeout или проверить интернет |

---

## 🔒 БЕЗОПАСНОСТЬ

### Куки
- Файл `config/cookies.json` **НЕ коммитить** в Git!
- Использовать отдельный аккаунт для автоматизации
- Регулярно обновлять куки

### Частота публикаций
- Не более 1-2 статей в час
- Добавить случайные задержки между публикациями
- Избегать подозрений в автоматизации

### Содержимое
- Проверять качество статей перед автопубликацией
- Убедиться в авторском праве на содержимое
- Соответствовать политике Яндекс Дзен

---

## 📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ

Все файлы находятся в `PRODUCTION_READY/`:

1. **Для новичков**: `INTRODUCTION.md` → `RUN_INSTRUCTIONS.md`
2. **Для отладки**: `UPDATE_INSTRUCTIONS.md` → `UPDATING_SELECTORS.md`
3. **Для автоматизации**: `AUTOMATION.md`
4. **Для безопасности**: `SECURITY_AND_RECOMMENDATIONS.md`
5. **Для FAQ**: `FAQ.md`

---

## 🗂️ ПАПКА for_delete/

Архив старых версий и дубликатов:
- Документация (дубли v1, v2, v3)
- Debug файлы (editor_page_source.html - 270 KB)
- Старые селекторы (SELECTORS_REFERENCE.md)
- Анализы и отчеты

**Можно удалить при необходимости очистки.**

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

- [ ] Установлены Node.js и npm
- [ ] `npm install` выполнен
- [ ] Браузер установлен (`npm run install-browsers`)
- [ ] Куки добавлены в `config/cookies.json`
- [ ] RSS-фид подготовлен в `public/feed.xml`
- [ ] Первый запуск с `headless: false` для проверки
- [ ] История публикаций пуста или актуальна
- [ ] Протестирована публикация одной статьи

---

## 🚨 QUANDO НУЖНА ПОМОЩЬ

### При изменении интерфейса Дзен:
1. Смотрите `UPDATE_INSTRUCTIONS.md`
2. Найдите новые селекторы через DevTools
3. Обновите `src/main.js`
4. Протестируйте перед полным запуском

### При ошибках:
1. Включите режим `headless: false`
2. Смотрите логи в консоли
3. Проверьте селекторы через DevTools
4. Обновите селекторы по инструкции

### Контрольные селекторы (для теста):
```javascript
// Тест в консоли браузера при открытом Дзене:
document.querySelector('[data-testid="add-publication-button"]') // Кнопка добавления
document.querySelector('[data-testid="publish-btn"]') // Кнопка публикации
```

---

## 📝 ВЕРСИОНИРОВАНИЕ

- **Production**: `PRODUCTION_READY/` ✅ (используется для работы)
- **Archive**: `for_delete/` (старые версии и дубликаты)
- **Backup**: Рекомендуется хранить резервные копии Git коммитами

---

## 🎯 ЗАКЛЮЧЕНИЕ

Это полностью рабочая система для автоматической публикации в Яндекс Дзен с:
- ✅ Проверенными селекторами
- ✅ Корректной обработкой HTML
- ✅ Системой отслеживания публикаций
- ✅ Полной документацией
- ✅ Инструкциями по обновлению при изменениях интерфейса

**Используйте `PRODUCTION_READY/` для работы. Остальное - архив и дубликаты.**

---

*Обновлено: 04.01.2025*
*Production Ready: ✅ YES*
