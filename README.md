# Dzen Auto-Publisher Ultimate

Полностью автоматизированный публишер для Яндекс Дзен, который публикует статьи из RSS-файла с полным текстом и изображениями, используя точные селекторы и правильную обработку HTML-тегов.

## Особенности

- 🚀 Автоматическая публикация статей из `public/feed.xml`
- 📸 Автоматическая вставка изображений
- 👤 Имитация человеческого ввода текста с задержками
- 🍪 Использование cookies для аутентификации
- 🛡️ Обход простых систем детекции автоматизации
- 📝 Заголовки и контент из RSS-файла
- 🧹 Правильная обработка HTML-тегов в тексте
- ⚡ Два шага публикации (в редакторе и в модальном окне)
- 🔍 Использование точных селекторов из dzen-schema.json

## Установка

1. Убедитесь, что у вас установлен Node.js
2. Установите зависимости:

```bash
npm install
```

3. Установите браузеры Playwright:

```bash
npx playwright install chromium
```

## Настройка

### 1. Подготовка куки

Для первого запуска вам нужно получить куки от Дзен:

1. Войдите в Дзен вручную
2. Откройте DevTools (F12)
3. Перейдите во вкладку Application (или Storage)
4. Найдите Cookies для домена `.dzen.ru`
5. Сохраните их в `!a_posts/config/cookies.json`

### 2. Подготовка feed.xml

Поместите ваш RSS-фид с статьями в `public/feed.xml`. Файл должен содержать:

```xml
<rss version="2.0">
  <channel>
    <title>Ваш канал</title>
    <item>
      <title><![CDATA[Заголовок статьи]]></title>
      <description><![CDATA[Описание статьи]]></description>
      <content:encoded><![CDATA[Текст статьи]]></content:encoded>
      <media:content url="https://ссылка-на-изображение.jpg" type="image/jpeg"/>
      <link>https://ссылка-на-статью</link>
      <pubDate>Дата публикации</pubDate>
    </item>
  </channel>
</rss>
```

## Использование

### Запуск публикации

```bash
cd !a_posts
node final_exact_publisher_updated.js
```

## Алгоритм работы

1. Чтение статьи из `public/feed.xml`
2. Открытие редактора Дзен
3. Заполнение заголовка (сначала!)
4. Заполнение тела статьи (потом!)
5. Нажатие Enter после вставки тела статьи
6. Вставка изображения по точному селектору
7. Нажатие первой кнопки публикации (в редакторе статьи)
8. Нажатие второй кнопки публикации (в модальном окне подтверждения)

## Важные особенности

- **Порядок заполнения**: Заголовок → Тело статьи (не наоборот!)
- **Два шага публикации**: 
  - Первая кнопка в редакторе статьи
  - Вторая кнопка в модальном окне подтверждения
- **Обработка HTML-тегов**: Все теги преобразуются в текст с правильными переносами строк
- **Использование точных селекторов**: `[data-testid="publish-btn"]` и другие из dzen-schema.json
- **Нажатие Enter**: После вставки тела статьи обязательно нажимается Enter

## Структура проекта

```
!a_posts/                    # Основная директория с публишером
├── config/
│   └── cookies.json        # Куки для аутентификации
├── public/
│   └── feed.xml            # RSS-фид с статьями
├── final_exact_publisher_updated.js  # Основной скрипт публикации
├── DOCUMENTATION_FINAL.md  # Полная документация
└── README_FINAL.md         # Краткое руководство
```

## Безопасность

- Куки хранятся локально
- Скрипт работает локально
- Рекомендуется использовать отдельный аккаунт

## Примечания

- Используйте с умом, чтобы избежать блокировок
- Проверяйте актуальность селекторов при обновлениях интерфейса
- Следите за обновлениями Дзен
- Обратите внимание на два шага публикации: в редакторе и в модальном окне