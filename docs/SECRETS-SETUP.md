# 🔐 GitHub Secrets Setup

## Быстрая настройка

### Шаг 1: Получите Gemini API ключ

1. Перейдите: https://aistudio.google.com/app/apikey
2. Нажмите **Create API key**
3. Выберите проект или создайте новый
4. Скопируйте ключ (начинается с `AIza...`)

### Шаг 2: Добавьте секрет в GitHub

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Заполните:
   ```
   Name: GEMINI_API_KEY
   Secret: AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
   ```
5. Нажмите **Add secret**

### Шаг 3: Проверьте что секрет работает

1. Перейдите в **Actions**
2. Выберите **Content Factory - Batch Articles**
3. Нажмите **Run workflow**
4. Настройте:
   - Count: `1`
   - Channel: `women-35-60`
   - Images: `true`
5. Нажмите **Run workflow**

**Ожидаемый результат:**
- Workflow успешно запустится
- Сгенерируется 1 статья
- Статья пройдет cleanup систему v6.0
- Статья будет закоммичена в репозиторий

## Проверка настройки

### В логах workflow вы должны увидеть:

```
✅ Title (Russian): "..."
✅ ARTICLE COMPLETE
📊 Metrics:
   - Episodes: 6
   - Characters: 19240
   - Phase 2 Score: 78/100
   - Anti-Detection: ✅ Applied

🧹 [Уровень 2] Final Article Cleanup Gate...
   ✅ No cleanup needed

🚪 [Уровень 3] Article Publish Gate...
   ✅ Article passed publish gate validation
```

### Если есть ошибки:

#### ❌ "API key not found" или "401 Unauthorized"

**Проблема:** Секрет не настроен или неверный ключ

**Решение:**
1. Проверьте что секрет называется **точно** `GEMINI_API_KEY` (case-sensitive)
2. Проверьте что ключ скопирован полностью
3. Проверьте что ключ активен в Google AI Studio

#### ❌ "429 Too Many Requests"

**Проблема:** Превышена квота API

**Решение:**
1. Подождите несколько минут
2. Проверьте лимиты: https://aistudio.google.com/app/apikey
3. Настройте меньшее количество статей (count: 1-5)

## Безопасность

### ✅ DO (Делайте)

- ✅ Храните API ключи **только** в GitHub Secrets
- ✅ Используйте `.env` файл **только локально**
- ✅ Проверьте что `.env` в `.gitignore`
- ✅ Ротируйте ключи если они были скомпрометированы

### ❌ DON'T (Не делайте)

- ❌ **НИКОГДА** не коммитьте `.env` файл
- ❌ **НИКОГДА** не вставляйте ключи в код
- ❌ **НИКОГДА** не шарьте ключи в Issues/PRs
- ❌ **НИКОГДА** не публикуйте ключи в логах

## Локальная разработка

### Создайте .env файл локально:

```bash
# 1. Скопируйте example
cp .env.example .env

# 2. Отредактируйте .env
nano .env

# 3. Добавьте ваш ключ
GEMINI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# v6.0: Cleanup настройки (опционально)
FINAL_CLEANUP_ENABLED=true
CLEANUP_THRESHOLD=medium
CLEANUP_MODEL=gemini-2.0-flash
CLEANUP_TEMPERATURE=0.3
CLEANUP_MAX_RETRIES=2
```

### Проверьте что .env НЕ в git:

```bash
# Проверка
git status

# .env НЕ должен появляться в списке
# Если появляется:
git rm --cached .env
echo ".env" >> .gitignore
```

## Дополнительные секреты (опционально)

Если в будущем понадобятся другие сервисы:

```
# Для других API
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Для базы данных
DATABASE_URL=postgresql://...

# Для CDN
CLOUDINARY_API_KEY=...
```

**Добавляются аналогично:** Settings → Secrets → New repository secret

## Troubleshooting

### Как посмотреть что секреты настроены?

1. Settings → Secrets and variables → Actions
2. Вы увидите список секретов (но **не** их значения)
3. Должен быть: `GEMINI_API_KEY` ✅

### Как обновить секрет?

1. Settings → Secrets and variables → Actions
2. Найдите `GEMINI_API_KEY`
3. Нажмите **Update**
4. Введите новое значение
5. **Save**

### Как удалить секрет?

1. Settings → Secrets and variables → Actions
2. Найдите `GEMINI_API_KEY`
3. Нажмите **Remove**
4. Подтвердите

## См. также

- [GitHub Actions Setup](./GITHUB-ACTIONS-SETUP.md)
- [v6.0 Cleanup System](./v6.0-cleanup-system.md)
- [Quick Start Guide](./CLEANUP-SYSTEM-README.md)
