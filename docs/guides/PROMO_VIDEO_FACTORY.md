# 🎥 Promo Video Factory (Documentation)

**Версия:** 1.0 (Gemini 3.0 + Google TTS)
**Статус:** ✅ Working

## 🚀 Описание
Система автоматически превращает текст статьи в динамичный видео-тизер (9:16) для Shorts/Reels/Дзен.Видео.

## 🛠 Компоненты системы

| Компонент | Файл | Технология | Роль |
|-----------|------|------------|------|
| **Оркестратор** | `src/orchestrator.ts` | TypeScript | Управляет всем процессом. |
| **Сценарист** | `src/ai/gemini_video_agent.ts` | **Gemini 3.0 Flash Preview** | Пишет сценарий, разбивает на сцены, придумывает промпты. |
| **Аудио (Brain)** | `src/audio/audio_service.ts` | **Gemini 3.0 Flash Preview** | Анализирует текст, задает скорость и настроение. |
| **Аудио (Voice)** | `src/audio/audio_service.ts` | **Google TTS API** | Генерирует надежный голос (fallback от Native TTS). |
| **Визуал** | `src/video/asset_manager.ts` | Canvas | Генерирует заглушки/слайды (готов к AI Image Gen). |
| **Рендер** | `src/video/renderer.ts` | **FFmpeg** | Монтирует клипы, делает Zoom Pan эффект. |

## ⚙️ Как запустить

### 1. Установка
```bash
cd promo_video
npm install
```

### 2. Настройка
Убедитесь, что в `.env` корне проекта есть `GEMINI_API_KEY`.

### 3. Генерация видео
```bash
# Из корня проекта
npx ts-node --skip-ignore promo_video/src/tools/test_full_pipeline.ts
```

### 4. Результат
Видео будет сохранено в:
*   Локально: `promo_video/output_test/final_video.mp4`
*   Публично (для превью): `public/promo_teaser.mp4`

## 🧩 Особенности реализации

### 🔊 Аудио ("Smart Director")
Мы используем гибридный подход. 
1. Gemini читает текст и говорит: "Это грустная сцена, читай со скоростью 0.85".
2. Мы передаем этот параметр в Google TTS.
Это дает **эмоциональный окрас** даже роботизированному голосу.

### 🎞 Видео ("Ken Burns")
Используется сложный FFmpeg фильтр `zoompan`.
Картинка плавно приближается (zoom in) в течение всей длительности аудио-фрагмента.

## 🔮 План развития
1.  Подключить **Rutube Uploader** (см. `docs/guides/RUTUBE_INTEGRATION.md`).
2.  Заменить Canvas-заглушки на **AI Images** (Imagen 3 / Midjourney).
3.  Добавить **Subtitles** (burn-in text) через FFmpeg.
