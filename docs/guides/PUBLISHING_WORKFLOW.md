# 🚀 Publishing Workflow (Playwright)

Этот гайд объясняет, как работает автоматическая публикация в Дзен и как её починить, если она сломалась.

---

## 🛠 Механизм работы

Публикатор (`!posts/zen_auto_publisher.js`) использует библиотеку **Playwright** для управления браузером Chromium.

**Основные этапы:**
1. **Чтение фида:** Парсит `public/feed.xml` через регулярные выражения (извлекает Title, Content, Image URL).
2. **Проверка истории:** Сверяется с `published_articles.txt`, чтобы не постить дубликаты.
3. **Авторизация:** Загружает куки из `config/cookies.json`.
4. **Эмуляция ввода:**
   - Переходит в редактор (`/profile/editor/potemki`).
   - Кликает "Написать статью".
   - **Заголовок:** Заполняет первым.
   - **Тело статьи:** Вставляет обработанный текст, затем имитирует нажатие `Enter`.
   - **Картинка:** Кликает на иконку изображения, вставляет URL обложки из фида.
5. **Публикация:** Двухэтапное нажатие кнопок "Опубликовать" (в редакторе и в модальном окне).

---

## 🔑 Ключевые селекторы (Дзен 2026)

Если публикация "зависла", скорее всего, Дзен изменил классы. Проверьте эти селекторы:

| Элемент | Селектор / Признак |
|---------|-------------------|
| **Кнопка "Добавить"** | `[data-testid="add-publication-button"]` |
| **Кнопка "Статья"** | `text="Написать статью"` |
| **Поле заголовка** | `input[placeholder*="заголовок" i]`, `div[contenteditable="true"]` (первое) |
| **Поле контента** | `div[contenteditable="true"]` (второе) |
| **Кнопка "Картинка"** | `button.article-editor-desktop--side-button__sideButton-1z[data-tip="Вставить изображение"]` |
| **Кнопка Публикации (1)** | `.article-editor-desktop--editor-header__publishButton-gc` |
| **Кнопка Публикации (2)** | `button[data-testid="publish-btn"][type="submit"]` |

---

## 🚑 Решение проблем (Troubleshooting)

### 1. "Ошибка авторизации / Выкидывает на логин"
- **Причина:** Истекли куки в `config/cookies.json`.
- **Решение:** 
  1. Зайти в Дзен в обычном браузере.
  2. С помощью расширения (напр. EditThisCookie) экспортировать куки в формате JSON.
  3. Сохранить их в `config/cookies.json`.

### 2. "Статья создается, но текст не вставляется"
- **Причина:** Дзен изменил структуру редактора (WYSIWYG).
- **Решение:** Проверить селектор `contenteditable`. В скрипте `zen_auto_publisher.js` есть логика перебора всех полей ввода — проверьте логи, сколько полей он находит.

### 3. "Не вставляется картинка"
- **Причина:** Дзен требует загрузки файла вместо URL или изменил окно вставки.
- **Решение:** Проверить селектор кнопки `data-tip="Вставить изображение"`.

---

## 📜 Файлы системы публикации
- `!posts/zen_auto_publisher.js` — Главный скрипт.
- `config/cookies.json` — Сессия.
- `published_articles.txt` — База опубликованных (простой текстовый файл).
- `public/feed.xml` — Источник данных.
