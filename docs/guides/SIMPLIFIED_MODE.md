# 🧠 Multi-Agent Story Generation (Simplified Mode)

Этот документ описывает логику работы мультиагентной системы в упрощенном режиме (v7.0), который является основным для получения качественного контента без артефактов.

---

## 🔄 Схема генерации (Step-by-Step)

### Шаг 1: Инициализация (MultiAgentService)
Система получает тему и параметры (эмоция, архетип).
- **RAG Check:** Ищет похожие успешные статьи в `parsed_examples.json`.
- **PlotBible Build:** Создает "ДНК истории" (Narrator Profile, Visual DNA).

### Шаг 2: Создание Outline (The Architect)
Центральный агент (`generateOutline`) создает структуру из 12 эпизодов.
- **Результат:** JSON с кратким описанием каждого шага, конфликтов и поворотов сюжета.

### Шаг 3: Параллельная генерация эпизодов (The Workers)
Для каждого из 12 эпизодов создается отдельный агент.
- **Входные данные:** `PlotBible` + `EpisodeOutline`.
- **Логика:** Агент пишет текст (2500+ символов), строго соблюдая голос автора и контекст предыдущих событий.
- **Simplified Mode:** Отключает "Phase 2 Anti-detection" и "Cleanup Gates", которые раньше могли ломать текст.

### Шаг 4: Сборка и Полировка (The Assembler)
- Склеивает эпизоды.
- Генерирует **Lede** (вступление) и **Finale** (мощное завершение).
- Проводит финальную чистку (`ContentSanitizer`).

---

## 🎭 Архетипы и Роли (v8.0)

Система больше не пишет "просто истории". Она использует **Архетипы**:
- **Mother Wins:** История о материнской силе и справедливости.
- **Gold Digger Trap:** Поучительная история о ложных ценностях.
- **Inheritance Reveal:** Драма вокруг семейных тайн и наследства.

Каждый архетип имеет свою структуру напряжения (Tension Arc), которая вшита в промпты.

---

## 🛠 Как восстановить процесс при сбоях

1. **Проблема с качеством:** Если текст кажется "роботизированным", проверьте `services/examplesService.ts`. Убедитесь, что база примеров `parsed_examples.json` актуальна.
2. **Проблема с последовательностью:** Если эпизоды противоречат друг другу, проверьте передачу `PlotBible` в `services/multiAgentService.ts`. Именно этот объект обеспечивает "память" системы.
3. **Проблема с форматом:** Если в тексте появились `[note]` или `**bold**`, проверьте `services/contentSanitizer.ts`.

---

## 📂 Связанные файлы
- `services/multiAgentService.ts` — Оркестратор.
- `services/episodeGeneratorService.ts` — Логика написания эпизодов.
- `services/plotBibleBuilder.ts` — Создание фундамента истории.
- `types/PlotBible.ts` — Структура данных ДНК истории.