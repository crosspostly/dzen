# 📈 Roadmap: Масштабирование и Анти-Детект (2026)

Этот документ описывает стратегию превращения одиночного скрипта публикации в масштабируемую систему для ведения сетки каналов (Multi-Account Setup) с защитой от теневых банов.

---

## 🎯 Цель
Автоматическое ведение **N-количества каналов** (5, 10, 20+) через GitHub Actions с минимальными рисками блокировок.

---

## 🚧 Этап 1: Сетевая Анонимность (Proxy Layer)
**Статус:** 🔴 Не начато (Используется Fallback: прямой IP GitHub + Captcha Solver)
**Приоритет:** Высокий

Использование IP-адресов дата-центров (Azure/GitHub) — главный триггер для антифрод-систем.

### Решение: Мобильные/Резидентные Прокси
Необходимо внедрить поддержку ротируемых прокси. Один прокси-канал может обслуживать всю ферму, если ротировать IP.

#### План внедрения:
1.  **Покупка:** Приобрести пакет "Mobile Proxy" или "Residential Proxy" (с оплатой за трафик, так как скрипт потребляет мало).
2.  **Секреты:** Добавить в GitHub Secrets переменную `DZEN_PROXY_URL` (формат: `http://user:pass@ip:port`).
3.  **Модификация Playwright (`src/main.js.ci`):**

```javascript
// Пример будущей реализации
const context = await browser.newContext({
  // ...
  proxy: process.env.DZEN_PROXY_URL ? {
    server: process.env.DZEN_PROXY_URL
  } : undefined,
});
```

---

## 🎭 Этап 2: Цифровые Личности (Digital Identity)
**Статус:** 🟡 Частично (Жестко заданный User-Agent)
**Приоритет:** Средний

Если 10 разных аккаунтов заходят с одним User-Agent и разрешением экрана — это подозрительно.

### Решение: Генератор Отпечатков (Fingerprints)
Каждый канал должен иметь "закрепленное" устройство.

#### План внедрения:
1.  **Конфигурация:** Создать папку `config/identities/`.
2.  **Файлы профилей:** Для каждого канала (напр. `potemki.json`) хранить:
    *   User-Agent (Chrome/Windows, Safari/macOS, etc.)
    *   Viewport (1920x1080, 1366x768...)
    *   Timezone & Locale (должны совпадать с Гео прокси!)
3.  **Интеграция:** При запуске скрипт должен загружать профиль конкретного канала.

---

## 🏗️ Этап 3: Архитектура Масштабирования (GitHub Matrix)
**Статус:** 🔴 Не начато (Работает только для 1 канала)
**Приоритет:** Высокий (для удобства)

Копировать файл workflow для каждого канала — плохая практика.

### Решение: Matrix Strategy
Использовать возможности GitHub Actions для параллельного запуска.

#### План внедрения:
1.  **Секреты:** Именовать куки по шаблону `DZEN_COOKIES_CHANNELNAME`.
2.  **Workflow (`auto-publish.yml`):**

```yaml
jobs:
  publish:
    strategy:
      matrix:
        channel: [potemki, history_life, psychology_daily] # Список каналов
      fail-fast: false # Если один упал, остальные продолжают
    steps:
      - name: Run Publisher
        run: node src/main.js.ci
        env:
          CHANNEL_ID: ${{ matrix.channel }}
          # Динамическая подстановка секретов
          DZEN_COOKIES_JSON: ${{ secrets[format('DZEN_COOKIES_{0}', matrix.channel)] }}
          # Identity конфиг берется по имени канала
```

---

## 🧠 Этап 4: Контентная Уникальность
**Статус:** 🟢 Работает (Базовая генерация)

Чтобы сетку не забанили за "контентный спам", каналы должны отличаться стилем.

### Решение: Архетипы ИИ
*   **Канал 1 (Драма):** Стиль "От первого лица", эмоциональный, короткие предложения.
*   **Канал 2 (Истории):** Стиль "Рассказчик", третий лицо, литературный язык.
*   **Канал 3 (Психология):** Стиль "Эксперт", аналитический, с выводами.

*Это настраивается на уровне промптов генерации (`generate-feed.js`), а не публикатора.*

---

## ✅ Итоговый Чек-лист (Путь к Ферме)

- [x] **v2.0 (Сейчас):** Надежная публикация одного канала (Anti-Detect + Captcha Solver).
- [ ] **v2.1 (Сетевой):** Покупка прокси -> Внедрение `DZEN_PROXY_URL`.
- [ ] **v2.2 (Архитектура):** Переход на Matrix Workflow -> Поддержка 2+ каналов.
- [ ] **v2.3 (Личности):** Рандомизация User-Agent под каждый канал.
