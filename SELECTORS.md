# 🤖 Селекторы для Публикации В Дзен

Теперь софокусированы и оптимизированы на основе стабильных данных атрибутов.

## 📄 Навигация и Сохранение

### 1. Постоянные селекторы (ОПТИМАЛЬНЫЕ)

| Действие | Селектор | Надёжность |
|--------|----------|----------|
| Кнопка "Добавить публикацию" | `[data-testid="add-publication-button"]` | 🔚 Очень высокая |
| Кнопка "Написать статью" | `text="Написать статью"` | 🔚 Очень высокая |
| Кнопка "Вставить изображение" | `button.article-editor-desktop--side-button__sideButton-1z[data-tip="Вставить изображение"]` | 🔚 Очень высокая |
| Кнопка подтверждения публикации | `button[data-testid="publish-btn"][type="submit"]` | 🔚 Очень высокая |
| Вставка нового артикля | `button:has-text("Опубликовать"):not([disabled])` | 🔚 Высокая |

---

## 🔍 Откуда брались селекторы

У нас есть несколько файлов-анализаторов для сканирования Dzen:

```
!posts/scripts/
└─ check_creation_buttons.js       # 🔍 Найдет новые кнопки
└─ dzen_editor_analyzer.js         # 🔍 Анализирует эдитор
└─ dzen_navigation_finder.js      # 🔍 Поиск навигации
└─ dzen_new_article_analyzer.js   # 🔍 Новые окна готовки вариантов
```

### Как обновить селекторы

1. **На Windows локально:**
   ```bash
   cd !posts/scripts
   node dzen_editor_analyzer.js
   ```

2. **Копируем новые селекторы в main.js.ci**

3. **Коммитим и пушим**

---

## 🔍 Почему эти селекторы надёжные

### НЕ йсть в них:
- ❌ Глубокие CSS-цепочки (`.a > .b > .c > .d`)
- ❌ Поэзия роман атрибутов (`[class*="some-pattern"]`)
- ❌ Надёжды на орден элементов (`:nth-child(3)`)

### Есть на них:
- ✅ `data-testid` - Наделены архитектурой приложения (редко меняются)
- ✅ `data-tip` - Специфичная тоолтип
- ✅ `text=` - По тексту кнопки (стабильно)
- ✅ `:not([disabled])` - Только активные кнопки

---

## 🔢 Поля для внесения данных

**Стратегия:** используем все доступные input/textarea/contenteditable, а не покап единых

1. **Поле 1** (contenteditable/input) = ЗАГОЛОВОК
2. **Поле 2** (contenteditable/textarea) = КОНТЕНТ (full text)

```javascript
const allInputs = await page.$$('input[type="text"], textarea, div[contenteditable="true"]');

// ЗАПОЛНЯЕМ ЗАГОЛОВОК
if (allInputs.length > 0) {
  await allInputs[0].fill(article.title);
}

// ЗАПОЛНЯЕМ КОНТЕНТ
if (allInputs.length > 1) {
  await allInputs[1].fill(processedContent);
}
```

---

## 🏃 Основные НОВОУВЕДОМЛЕНИЕ

### От сплосных селекторов:

```diff
- button.article-editor-desktop--editor-header__editBtn-44.article-editor-desktop--base-button__rootElement-75
+ button:has-text("Опубликовать"):not([disabled])
```

```diff
- input[placeholder="Search..."]:not([value])
+ input[type="text"]
```

### Что ждать от браузера:

```javascript
// ОПТИМАЛЬНО: Короткие ждания
await page.waitForTimeout(3000);

// ПОНУЖДАЮЩЕЕ ждание DOM:
await page.waitForSelector('button[data-testid="publish-btn"]', { timeout: 5000 });

// Отправка кнаопки и ждание навигации:
await page.click('button'); 
await page.waitForNavigation({ waitUntil: 'networkidle' });
```

---

## 🔐 По контролю

После обновления селекторов:

1. Протестируем локально
2. Протестируем в GitHub Actions
3. Набвлюдаем результаты в Actions tab
4. Подождем двых циклов тестования (вт е быно с вероятностью непредвиденные рроры)
5. У жинся однас - работают!

---

**Обновлено:** 2026-01-04 10:27
